<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyCalculator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyCalculator" parent="Base.Theme.MyCalculator" />
    <style name="OperatorButton" parent="CommonButton">
        <item name="android:textColor">@color/yellow</item>


    </style>

    <style name="digitButton" parent="CommonButton">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="CommonButton" parent="@style/Widget.Material3.ExtendedFloatingActionButton.Icon.Primary">
        <item name="android:layout_width">72dp</item>
        <item name="android:layout_height">72dp</item>
        <item name="cardCornerRadius">36dp</item>
        <item name="android:backgroundTint">@color/white</item>
        <item name="android:textSize">32dp</item>
        <item name="android:layout_margin">12dp</item>

    </style>
</resources>